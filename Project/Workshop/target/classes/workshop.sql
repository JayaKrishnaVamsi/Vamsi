
CREATE TABLE workshop (
  
	work_id varchar(45) PRIMARY KEY, 
	topic varchar(45), 
	venue varchar(45), 
	Date date, 
	organiser varchar(45), 
	reg_fee varchar(45), 
	location varchar(45)
);